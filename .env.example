# --- Front (nécessaires partout : local ET Vercel) ---
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
ANTHROPIC_API_KEY=

# --- Ingestion (local uniquement — jamais sur Vercel) ---
# Active le bouton "Rechercher" + la route /api/rechercher en local.
# Ne JAMAIS mettre à true sur Vercel (le front seul y est déployé).
NEXT_PUBLIC_ENABLE_INGEST=true
IMAP_USER=
IMAP_PASSWORD=
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_MAILBOX=immo-alertes
FETCH_LIMIT=10
